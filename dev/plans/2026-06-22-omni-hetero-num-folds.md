# Expose `num.folds` in Sequential RATE — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a validated, user-facing `num.folds` parameter (default 5) to `omni_hetero()`'s Sequential RATE test, forwarded from `summary.causal_forest()`.

**Architecture:** `omni_hetero()` gains a `num.folds` arg, validated up front, threaded into the inner `rate_sequential()` closure and the upfront size-check warning (removing a duplicated magic `5L`). `summary.causal_forest()` forwards its existing `...` to `omni_hetero()`. Default 5 preserves all current behavior and snapshots.

**Tech Stack:** R package; `grf`, `rlang`; `testthat` edition 3; `roxygen2` (markdown); `devtools`.

**Design doc:** `dev/specs/2026-06-22-omni-hetero-num-folds-design.md`

---

## File Structure

- `R/omni_hetero.R` — add param + validation; remove duplicate `.seq.num.folds`; pass `num.folds` to the closure; roxygen `@param num.folds`.
- `R/summary.causal_forest.R` — forward `...` to `omni_hetero()`; roxygen `@param ...`.
- `tests/testthat/test-omni_hetero.R` — append validation, propagation, scaling, and summary-passthrough tests.
- `NEWS.md` — development-version entry.
- `man/omni_hetero.Rd`, `man/summary.causal_forest.Rd` — regenerated by `devtools::document()` (never hand-edited).

Single-pass single test file:
`Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R")'`

---

## Task 0: Branch

- [ ] **Step 1: Create a feature branch** (repo default branch is `main`)

```bash
cd ~/Dropbox/UtopiaPlanitia
git checkout -b feat/omni-hetero-num-folds
```

---

## Task 1: Signature + validation

**Files:**
- Modify: `R/omni_hetero.R:102` (signature) and after `R/omni_hetero.R:104-106` (validation block)
- Test: `tests/testthat/test-omni_hetero.R` (append)

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-omni_hetero.R`:

```r
test_that("omni_hetero exposes num.folds parameter with default 5", {
  expect_true("num.folds" %in% names(formals(omni_hetero)))
  expect_equal(eval(formals(omni_hetero)$num.folds), 5)
})

test_that("omni_hetero rejects invalid num.folds", {
  cf <- make_small_null_cf(n = 200, num.trees = 100, seed = 1995)
  expect_error(omni_hetero(cf, num.folds = 2),       "num.folds")
  expect_error(omni_hetero(cf, num.folds = 0),       "num.folds")
  expect_error(omni_hetero(cf, num.folds = 5.5),     "num.folds")
  expect_error(omni_hetero(cf, num.folds = c(3, 4)), "num.folds")
  expect_error(omni_hetero(cf, num.folds = "5"),     "num.folds")
  expect_error(
    omni_hetero(cf, num.folds = nrow(cf$X.orig) + 1L),
    "cannot exceed"
  )
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R", desc = NULL)'`
Expected: the two new tests FAIL — `num.folds` is not yet an argument, so `formals()` lacks it and the `expect_error` calls do not error (they run the full test or error on the unknown argument with a different message).

- [ ] **Step 3: Add the parameter and validation**

In `R/omni_hetero.R`, change the signature (line 102):

```r
omni_hetero <- function(c.forest, seed = 1995, min_fold_n = 100, num.folds = 5) {
```

Immediately after the existing class check (the `if (!inherits(c.forest, "causal_forest")) { stop(...) }` block, lines 104–106) and before `set.seed(seed)`, insert:

```r
  # Validate num.folds: a single integer K >= 3 (5 or more recommended).
  # K = 2 always yields < 2 usable folds (the loop runs k in 2:K), so the
  # Sequential RATE p-value would always be NA -- reject it outright.
  if (!is.numeric(num.folds) || length(num.folds) != 1L ||
      !is.finite(num.folds) || num.folds != round(num.folds) ||
      num.folds < 3) {
    stop("num.folds must be a single integer >= 3 (5 or more recommended).",
         call. = FALSE)
  }
  num.folds <- as.integer(num.folds)
  # More folds than observations leaves high fold-ids unassigned in
  # sample(rep(1:num.folds, length = n)), so samples.by.fold[[k]] is missing
  # and the fold loop errors. Guard against it.
  if (num.folds > nrow(c.forest$X.orig)) {
    stop("num.folds (", num.folds, ") cannot exceed the number of ",
         "observations (", nrow(c.forest$X.orig), ").", call. = FALSE)
  }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R")'`
Expected: both new tests PASS. All pre-existing tests still PASS (default `num.folds = 5` preserves behavior).

- [ ] **Step 5: Commit**

```bash
git add R/omni_hetero.R tests/testthat/test-omni_hetero.R
git commit -m "feat(omni_hetero): add validated num.folds parameter (default 5)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Thread num.folds through the RATE computation

**Files:**
- Modify: `R/omni_hetero.R:441-443` (size-check warning) and `R/omni_hetero.R:455` (call site)
- Test: `tests/testthat/test-omni_hetero.R` (append)

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-omni_hetero.R`:

```r
test_that("omni_hetero respects num.folds in the RATE payload", {
  set.seed(1995)
  n <- 500; p <- 5
  X <- matrix(rnorm(n * p), n, p)
  W <- rbinom(n, 1, 0.5)
  Y <- rnorm(n)
  cf <- grf::causal_forest(X, Y, W, num.trees = 100)

  result <- suppressWarnings(suppressMessages(omni_hetero(cf, num.folds = 3)))
  rate <- attr(result, "rate")
  expect_equal(rate$num_folds, 3)
  # loop runs k in 2:num.folds -> at most num.folds - 1 per-fold rows
  expect_lte(nrow(rate$folds_df), 2L)
})

test_that("omni_hetero size-check warning scales with num.folds", {
  set.seed(1995)
  n <- 500; p <- 5
  X <- matrix(rnorm(n * p), n, p)
  W <- rbinom(n, 1, 0.5)
  Y <- rnorm(n)
  cf <- grf::causal_forest(X, Y, W, num.trees = 100)

  collect <- function(folds) {
    msgs <- character(0)
    suppressMessages(withCallingHandlers(
      omni_hetero(cf, num.folds = folds),
      warning = function(w) {
        msgs <<- c(msgs, conditionMessage(w)); invokeRestart("muffleWarning")
      }))
    msgs
  }
  # n/5 = 100 is NOT < min_fold_n (100) and n >= 400 -> no size warning
  expect_false(any(grepl("Sequential RATE may be unstable",
                         collect(5), fixed = TRUE)))
  # n/6 = 83.3 < 100 -> size warning fires (proves the count is dynamic)
  expect_true(any(grepl("Sequential RATE may be unstable",
                        collect(6), fixed = TRUE)))
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R")'`
Expected: `respects num.folds` FAILS (`rate$num_folds` is 5, the hardcoded default in the closure) and `size-check warning scales` FAILS for `collect(6)` (the warning still uses the magic `.seq.num.folds <- 5L`, so `n/5 = 100` does not trip it).

- [ ] **Step 3: Wire num.folds into the size check and the call site**

In `R/omni_hetero.R`, replace the size-check block (lines ~441–453):

```r
  # Upfront size check (Task A requirement): warn when Sequential RATE
  # likely unstable. Uses default num.folds = 5 from rate_sequential().
  .seq.num.folds <- 5L
  .n <- nrow(X)
  if (.n < 400L || (.n / .seq.num.folds) < min_fold_n) {
    warning(
      "Sequential RATE may be unstable at this sample size (n = ", .n,
      ", n/num.folds = ", round(.n / .seq.num.folds, 1),
      "; min_fold_n = ", min_fold_n, "). Training folds may be too small ",
      "for the per-fold CATE forest to detect heterogeneity, which can ",
      "produce degenerate RATE statistics. Consider the Calibration test ",
      "(Chernozhukov et al., 2018) or the OOB RATE heuristics instead.",
      call. = FALSE
    )
  }

  rate_result <- rate_sequential(X, Y, W)
```

with:

```r
  # Upfront size check (Task A requirement): warn when Sequential RATE
  # likely unstable. Scales with the user-supplied num.folds.
  .n <- nrow(X)
  if (.n < 400L || (.n / num.folds) < min_fold_n) {
    warning(
      "Sequential RATE may be unstable at this sample size (n = ", .n,
      ", n/num.folds = ", round(.n / num.folds, 1),
      "; min_fold_n = ", min_fold_n, "). Training folds may be too small ",
      "for the per-fold CATE forest to detect heterogeneity, which can ",
      "produce degenerate RATE statistics. Consider the Calibration test ",
      "(Chernozhukov et al., 2018) or the OOB RATE heuristics instead.",
      call. = FALSE
    )
  }

  rate_result <- rate_sequential(X, Y, W, num.folds = num.folds)
```

(The inner closure keeps its own `num.folds = 5` default as a harmless fallback; it is now always passed explicitly. The `num_folds` payload field is already populated from the closure arg.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R")'`
Expected: all tests PASS, including the two new ones.

- [ ] **Step 5: Commit**

```bash
git add R/omni_hetero.R tests/testthat/test-omni_hetero.R
git commit -m "feat(omni_hetero): thread num.folds into RATE + size check

Removes the duplicated magic 5L so the size-check warning scales with
the chosen fold count.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Forward num.folds from summary.causal_forest()

**Files:**
- Modify: `R/summary.causal_forest.R:71` (omni_hetero call)
- Test: `tests/testthat/test-omni_hetero.R` (append)

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-omni_hetero.R`:

```r
test_that("summary.causal_forest forwards num.folds to omni_hetero", {
  set.seed(1995)
  n <- 500; p <- 5
  X <- matrix(rnorm(n * p), n, p)
  W <- rbinom(n, 1, 0.5)
  Y <- rnorm(n)
  cf <- grf::causal_forest(X, Y, W, num.trees = 100)

  s <- suppressWarnings(suppressMessages(summary(cf, num.folds = 3)))
  rate <- attr(s$heterogeneity, "rate")
  expect_equal(rate$num_folds, 3)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R")'`
Expected: FAIL — `summary.causal_forest()` drops `...`, so `omni_hetero()` runs with the default 5 and `rate$num_folds` is 5, not 3.

- [ ] **Step 3: Forward the dots**

In `R/summary.causal_forest.R`, change line 71 from:

```r
  hetero <- omni_hetero(object, seed = seed)
```

to:

```r
  hetero <- omni_hetero(object, seed = seed, ...)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-omni_hetero.R")'`
Expected: PASS. All other tests still PASS.

- [ ] **Step 5: Commit**

```bash
git add R/summary.causal_forest.R tests/testthat/test-omni_hetero.R
git commit -m "feat(summary.causal_forest): forward ... to omni_hetero (num.folds)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Documentation

**Files:**
- Modify: `R/omni_hetero.R` (roxygen block above line 102), `R/summary.causal_forest.R` (roxygen block)
- Modify: `NEWS.md` (top, development version)
- Regenerate: `man/omni_hetero.Rd`, `man/summary.causal_forest.Rd`

- [ ] **Step 1: Add the `@param num.folds` roxygen line to `omni_hetero()`**

In `R/omni_hetero.R`, in the roxygen block, immediately after the `@param min_fold_n ...` block and before `@return`, add:

```r
#' @param num.folds Integer number of folds K for the sequential RATE
#'   cross-validation (Wager, 2024). Must be a single integer `>= 3`
#'   (`K = 2` cannot yield the two usable folds the test requires) and no
#'   greater than the number of observations. Default is `5`; `5` or more
#'   is recommended. Larger K gives each per-fold CATE forest more training
#'   data, but more folds at small n raises the risk of degenerate folds
#'   (see `min_fold_n`).
```

- [ ] **Step 2: Add the `@param ...` roxygen line to `summary.causal_forest()`**

In `R/summary.causal_forest.R`, in the roxygen block, add (or update) a `@param ...` entry:

```r
#' @param ... Additional arguments forwarded to [omni_hetero()] (for
#'   example `num.folds` or `min_fold_n`).
```

- [ ] **Step 3: Add a NEWS.md entry**

In `NEWS.md`, under the top `# UtopiaPlanitia (development version)` heading, add a new section:

```markdown
## `omni_hetero()` exposes the Sequential RATE fold count

* **New `num.folds` argument** (default `5`) controls the number of folds
  K in the Sequential RATE test (Wager, 2024). Must be an integer `>= 3`
  and no greater than the sample size; `5` or more is recommended. The
  upfront sample-size warning now scales with the chosen fold count.
* **`summary.causal_forest()` forwards `...`** to `omni_hetero()`, so
  `summary(cf, num.folds = 10)` works.
```

- [ ] **Step 4: Regenerate documentation**

Run: `Rscript -e 'devtools::document()'`
Expected: `man/omni_hetero.Rd` and `man/summary.causal_forest.Rd` updated; `NAMESPACE` unchanged (no new exports).

- [ ] **Step 5: Commit**

```bash
git add R/omni_hetero.R R/summary.causal_forest.R NEWS.md man/
git commit -m "docs(omni_hetero): document num.folds and summary passthrough

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Full verification

**Files:** none (verification only; commit only if lint auto-fixes are needed)

- [ ] **Step 1: Run the full test suite**

Run: `Rscript -e 'devtools::test()'`
Expected: all tests PASS, including the new `num.folds` tests. No snapshot diffs (default behavior unchanged).

- [ ] **Step 2: Lint**

Run: `Rscript -e 'lintr::lint_package()'`
Expected: no new lints in `R/omni_hetero.R` or `R/summary.causal_forest.R` (watch line length and `<-` style in the inserted blocks).

- [ ] **Step 3: R CMD check**

Run: `Rscript -e 'devtools::check()'`
Expected: `0 errors | 0 warnings | 0 notes` (or only pre-existing notes unrelated to this change).

- [ ] **Step 4: Commit any lint fixes**

```bash
git add -A
git commit -m "style(omni_hetero): clear lints from num.folds change

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

(Skip if Steps 1–3 were clean.)

---

## Self-Review (author checklist — already run)

- **Spec coverage:** signature (T1), validation lower/upper bounds (T1), size-check scaling + call-site wiring + magic-5 removal (T2), summary `...` passthrough (T3), roxygen + NEWS + `document()` (T4), full verify (T5). All spec sections mapped.
- **Placeholders:** none — every code/test step has literal content.
- **Type/name consistency:** `num.folds` (arg) ↔ `num_folds` (payload field, pre-existing) used consistently; `rate$num_folds`, `s$heterogeneity`, `cf$X.orig` match existing code read during design.
