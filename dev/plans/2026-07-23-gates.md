# gates() + plot_gates() — Implementation Plan

**Goal:** Export Chernozhukov GATES for fitted causal forests with a matching plot.

**Spec:** `dev/specs/2026-07-23-gates-design.md`

## Files

| Action | Path |
| --- | --- |
| Create | `R/gates.R` |
| Create | `R/gates_methods.R` |
| Create | `R/plot_gates.R` |
| Create | `tests/testthat/test-gates.R` |
| Modify | `NEWS.md`, `_pkgdown.yml`, `tests/testthat/test-fixes-20260511.R` |
| Regenerate | `man/*.Rd`, `NAMESPACE` via `devtools::document()` |

## Tasks

1. Implement `.gates_quantile_group()` + `.gates_fit()` (classic + HT paths).
2. Export `gates()` returning `cf_gates`.
3. Export `print.cf_gates`, `summary.cf_gates`, `plot_gates()`.
4. Tests + API guard.
5. Document, run `devtools::test()`, open PR from `feat/gates`.
