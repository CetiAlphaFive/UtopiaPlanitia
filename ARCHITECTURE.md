# Architecture — UtopiaPlanitia

> Generated by scriber for run `2026-07-06-loco-drop-conformal` on
> 2026-07-06.

## Overview

UtopiaPlanitia is an R package (built on `grf`) providing variable
importance, heterogeneity tests, nuisance-estimation backends, and
visualization tools for causal forests. Most functions analyze
heterogeneous treatment effect (HTE) estimates from a fitted
[`grf::causal_forest`](https://rdrr.io/pkg/grf/man/causal_forest.html),
though
[`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md)
and its supporting infrastructure work on plain outcome models
(`ranger`/`grf` regression, probability, and classification forests)
rather than causal forests. The package is organized into three
functional clusters — nuisance estimation backends, diagnostics/tests,
and visualization — all consumed via S3 dispatch
([`summary()`](https://rdrr.io/r/base/summary.html),
[`plot()`](https://rdrr.io/r/graphics/plot.default.html)) on a fitted
forest object. `grf` and `rlang` are the only hard `Imports`; everything
else (ggplot2, ranger, glmnet, xgboost, TabPFN, the mlr3 stack,
`conformalInference`-style oracles, etc.) is `Suggests` and guarded with
[`requireNamespace()`](https://rdrr.io/r/base/ns-load.html).

As of this run,
[`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md)
no longer depends on the non-CRAN `conformalInference` package for any
code path — its one remaining non-trivial split-mode case (per-variable,
ranger, regression, `loss = "abs"`) now falls through to the package’s
own `loco_custom_split()` sample-splitting + Z/Wilcoxon inference
routine, which already handled every other split-mode case.

------------------------------------------------------------------------

## Module Structure

``` mermaid
%%{init: {'theme': 'neutral'}}%%
graph TD
    subgraph Nuisance["Nuisance-Estimation Backends"]
        AUTOCF["autocf.R — autocf()"]
        GLMCF["glmcf.R — glmcf()"]
        TABCF["tabcf.R — tabcf()"]
        SETUPTOK["setup_tabpfn_token.R"]
    end

    subgraph Diag["Diagnostics & Tests"]
        LOCO["loco.R — loco()"]
        CFLOCO["cf_loco.R — cf_loco()"]
        VIMP["compute_vimp.R"]
        CFLOCOM["cf_loco_methods.R"]
        CFPERM["cf_perm.R — cf_perm()"]
        CFPERMM["cf_perm_methods.R"]
        OMNI["omni_hetero.R — omni_hetero()"]
    end

    subgraph Viz["Visualization"]
        PLOTCF["plot.causal_forest.R"]
        SUMMCF["summary.causal_forest.R"]
        PLOTDIAG["plot_diag.R"]
        PLOTPDP["plot_pdp.R — plot_pdp()"]
        PLOTRANK["plot_rank.R — plot_rank()"]
        PLOTSCAT["plot_scatter.R — plot_scatter()"]
        PLOTRATE["plot_rate.R"]
        PLOTBLP["plot_blp.R"]
        PLOTOMNI["plot.omni_hetero.R"]
        UTOPLOT["utopia_plot.R"]
        VISTYLE["vi_plot_style.R"]
    end

    subgraph Core["Package Infra"]
        ZZZ["zzz.R — .onLoad/.onAttach"]
    end

    CFLOCO --> VIMP
    CFLOCO --> CFLOCOM
    CFPERM --> CFPERMM
    PLOTCF --> PLOTDIAG
    PLOTCF --> PLOTPDP
    PLOTCF --> PLOTSCAT
    PLOTCF --> PLOTRANK
    PLOTOMNI --> PLOTBLP
    PLOTOMNI --> PLOTRATE
    CFLOCOM --> VISTYLE
    CFPERMM --> VISTYLE
    PLOTPDP --> VISTYLE
    CFLOCOM --> UTOPLOT
    CFPERMM --> UTOPLOT
    PLOTDIAG -.optional.-> MLBAL["MLbalance::random_check()"]
    AUTOCF -.optional.-> MLR3["mlr3 stack (AutoTuner)"]
    AUTOCF -.optional.-> DBARTS["dbarts::bart()"]
    AUTOCF -.optional.-> TABPFN["TabPFN / TABPFN_TOKEN"]
    LOCO -.optional.-> SURVIVAL["survival (loco.R helpers)"]

    style LOCO fill:#1e90ff,stroke:#1565c0,color:#fff
```

> One unified diagram grouped by the three functional clusters from
> `CLAUDE.md`, plus a small “Package Infra” layer for `zzz.R`. Dashed
> edges mark optional (`Suggests`-only) dependencies. `LOCO`
> (`R/loco.R`) is highlighted blue — its split-mode code path changed in
> this run (the `conformalInference`-calling branch was deleted; the
> non-CRAN `conformalInference` dependency itself is gone from
> `Suggests` and from every reachable code/doc path).

### Module Reference

| Module / File | Layer | Purpose | Key Exports | Changed |
|----|----|----|----|----|
| `R/autocf.R` | Nuisance | Auto-selected nuisance causal forest; cross-fits a candidate pool (`grf`, `glmnet`, `xgboost`, `tabpfn`, `bart`) and picks the best per nuisance by K-fold weighted MSE | [`autocf()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/autocf.md) | no |
| `R/glmcf.R` | Nuisance | Nuisances via `cv.glmnet` | [`glmcf()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/glmcf.md) | no |
| `R/tabcf.R` | Nuisance | Nuisances via TabPFN | [`tabcf()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/tabcf.md) | no |
| `R/setup_tabpfn_token.R` | Nuisance | Sets up the `TABPFN_TOKEN` env var | [`setup_tabpfn_token()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/setup_tabpfn_token.md) | no |
| `R/loco.R` | Diagnostics | LOCO variable importance for `ranger`/`grf` **outcome** forests (regression/probability/classification); auto-detects backend; split-sample and OOB modes | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) (+ internal `loco_custom_split()`, `detect_backend()`, `oob_loss()`, etc.) | **yes** — `conformalInference`-calling branch deleted; per-variable/regression/`loss="abs"` split case now falls through to `loco_custom_split()` |
| `R/cf_loco.R` + `R/compute_vimp.R` | Diagnostics | LOCO variable importance for **causal** forests | [`cf_loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/cf_loco.md) | no |
| `R/cf_loco_methods.R` | Diagnostics | S3 `print`/`summary`/`plot` for `cf_loco` objects | (S3 methods) | no |
| `R/cf_perm.R` | Diagnostics | PermuCATE conditional-permutation variable importance (Paillard et al., 2025) | [`cf_perm()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/cf_perm.md) | no |
| `R/cf_perm_methods.R` | Diagnostics | S3 `print`/`summary`/`plot` for `cf_perm` objects | (S3 methods) | no |
| `R/omni_hetero.R` | Diagnostics | Omnibus HTE test battery (calibration, high/low CATE, RATE) | [`omni_hetero()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/omni_hetero.md) | no |
| `R/plot.causal_forest.R` | Visualization | S3 [`plot()`](https://rdrr.io/r/graphics/plot.default.html) dispatcher (`type = "diag"/"pdp"/"scatter"/"rank"`) | `plot.causal_forest` | no |
| `R/summary.causal_forest.R` | Visualization | S3 [`summary()`](https://rdrr.io/r/base/summary.html) / `print.summary()` for causal forests | `summary.causal_forest`, `print.summary.causal_forest` | no |
| `R/plot_diag.R` | Visualization | Multi-panel diagnostic plot (optional `MLbalance` balance panel) | (internal panels) | no |
| `R/plot_pdp.R` | Visualization | 1-way/2-way partial dependence plots; `subgroup = TRUE` AIPW mode | [`plot_pdp()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/plot_pdp.md) | no |
| `R/plot_rank.R` | Visualization | Ranked CATEs with CIs | [`plot_rank()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/plot_rank.md), [`rank_plot()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/plot_rank.md) | no |
| `R/plot_scatter.R` | Visualization | Individual OOB CATEs vs. a covariate | [`plot_scatter()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/plot_scatter.md) | no |
| `R/plot_rate.R` | Visualization | TOC/RATE curve panel | (internal, used by `plot.omni_hetero.R`) | no |
| `R/plot_blp.R` | Visualization | BLP calibration panel | (internal, used by `plot.omni_hetero.R`) | no |
| `R/plot.omni_hetero.R` | Visualization | S3 `print`/`plot` for `omni_hetero` objects | `plot.omni_hetero`, `print.omni_hetero` | no |
| `R/utopia_plot.R` | Visualization | Shared S3 `print` method wrapping plot objects | `print.utopia_plot` | no |
| `R/vi_plot_style.R` | Visualization | Shared VI-plot theme/style helpers used by `cf_loco`/`cf_perm`/`plot_pdp` | (internal helpers) | no |
| `R/zzz.R` | Infra | Package `.onLoad`/`.onAttach` hooks | (none exported) | no |

`R/old/` is archived/pruned code (not built). `dev/` and `runs/` are
agent/scratch working dirs (build-ignored). `tools/blp_smoke.R` is a
standalone `Rscript` smoke test for `plot_blp.R`/`omni_hetero.R`.

------------------------------------------------------------------------

## Function Call Graph

``` mermaid
%%{init: {'theme': 'neutral'}}%%
graph TD
    loco["loco()"]
    loco --> detect_backend["detect_backend()"]
    loco --> treetype["treetype_for_backend()"]
    loco --> pred_names["pred_names_for()"]
    loco --> extract_hp["extract_grf_hp()"]
    loco --> oob_loss["oob_loss()"]
    loco --> loco_split["loco_custom_split()"]
    loco --> grf_refit["grf_refit()"]

    loco_split --> fit_one["fit_one() (closure)"]
    loco_split --> predict_one["predict_one() (closure)"]
    loco_split --> residuals_one["residuals_one() (closure)"]
    fit_one --> ranger_fit["ranger::ranger()"]
    fit_one --> grf_refit
    residuals_one --> ztest["stats::pnorm/qnorm (method='z')"]
    residuals_one --> wilcoxtest["stats::wilcox.test(exact=FALSE) (method='wilcox')"]

    oob_loss --> ranger_fit

    style loco fill:#1e90ff,stroke:#1565c0,color:#fff
    style loco_split fill:#1e90ff,stroke:#1565c0,color:#fff
```

> Blue nodes changed in this run.
> [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md)’s
> split-mode branch that used to call the external
> [`conformalInference::loco()`](https://rdrr.io/pkg/conformalInference/man/loco.html)
> (its own internal
> [`sample()`](https://rdrr.io/r/base/sample.html)/[`ranger::ranger()`](http://imbs-hl.github.io/ranger/reference/ranger.md)/`my.z.test`/`my.wilcox.test`
> sequence) was deleted; every split-mode case (including the migrated
> one) now calls the in-package `loco_custom_split()`, which was already
> used for every other split-mode configuration and is unmodified in its
> own logic — only its set of callers grew (implicitly, since the branch
> above it was removed rather than `loco_custom_split()` itself being
> touched).

### Function Reference

| Function | Defined In | Called By | Calls | Changed | Purpose |
|----|----|----|----|----|----|
| [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) | `R/loco.R` | user / exported | `detect_backend`, `treetype_for_backend`, `pred_names_for`, `extract_grf_hp`, `oob_loss`, `loco_custom_split`, `grf_refit` | **yes** | Public entry point; validates args, recovers hyperparameters, dispatches to split-mode (`loco_custom_split`) or OOB mode |
| `loco_custom_split()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) (unconditionally, for every split-mode case as of this run) | [`ranger::ranger`](http://imbs-hl.github.io/ranger/reference/ranger.md), `grf_refit`, [`stats::pnorm`](https://rdrr.io/r/stats/Normal.html)/`qnorm`, [`stats::wilcox.test`](https://rdrr.io/r/stats/wilcox.test.html) | no (callers changed, body did not) | Sample-splits data, refits full + per-target reduced models, computes one-sided Z or Wilcoxon inference on loss-residual differences |
| `detect_backend()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) | — | no | Maps model class to `"ranger"`/`"grf_reg"`/`"grf_brf"`/`"grf_prob"` or errors |
| `treetype_for_backend()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) | — | no | Resolves treetype string (`"Regression"`, `"Probability estimation"`, `"Classification"`) |
| `pred_names_for()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) | — | no | Predictor names in fitted-model column order |
| `extract_grf_hp()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) | — | no | Recovers grf hyperparameters for refitting |
| `grf_refit()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md), `loco_custom_split()` | [`grf::regression_forest`](https://rdrr.io/pkg/grf/man/regression_forest.html)/etc. | no | Refits a grf model with recovered hyperparameters on a data subset |
| `oob_loss()` | `R/loco.R` | [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md) (OOB mode only) | — | no | Computes OOB prediction-error loss for a fitted model |
| [`cf_loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/cf_loco.md) | `R/cf_loco.R` | user / exported | `compute_vimp` | no | LOCO variable importance for causal forests (separate from [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md)) |

------------------------------------------------------------------------

## Data Flow

``` mermaid
%%{init: {'theme': 'neutral'}}%%
graph TD
    Input["loco(model, data, ..., loss, split, method)"] --> Validate["Validate args + detect backend/treetype"]
    Validate --> Recover["Recover hyperparameters (hp.args)"]
    Recover --> Mode{{"split = TRUE?"}}
    Mode -- yes --> BuildXY["Build x / y_train matrix"]
    BuildXY --> CustomSplit["loco_custom_split(): sample-split, refit full + per-target, compute residual deltas"]
    CustomSplit --> InfMethod{{"method?"}}
    InfMethod -- z --> ZTest["Normal-theory Z-test (pnorm/qnorm)"]
    InfMethod -- wilcox --> WilcoxTest["Wilcoxon signed-rank (exact = FALSE)"]
    ZTest --> Assemble["Assemble output frame"]
    WilcoxTest --> Assemble
    Mode -- no --> OOBLoop["OOB mode: per-target refit + oob_loss() delta"]
    OOBLoop --> Assemble
    Assemble --> Output["Sorted data.frame: variable, importance, ci.lower, ci.upper, p.value, method, loss"]

    style CustomSplit fill:#1e90ff,stroke:#1565c0,color:#fff
```

> Vertical flowchart. The `use_conformal` branch that used to sit
> between “Build x / y_train matrix” and an external
> [`conformalInference::loco()`](https://rdrr.io/pkg/conformalInference/man/loco.html)
> call has been removed — every split-mode path now flows through
> `loco_custom_split()` (highlighted), which was already the destination
> for every other split-mode configuration.

------------------------------------------------------------------------

## Architectural Patterns

- **S3 dispatch is the public interface.** A fitted `causal_forest`
  flows into [`summary()`](https://rdrr.io/r/base/summary.html) /
  [`plot()`](https://rdrr.io/r/graphics/plot.default.html); LOCO objects
  (`cf_loco`) and test objects (`omni_hetero`) carry their own
  print/summary/plot methods.
  [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md)
  itself returns a plain `data.frame` (no S3 class) — it operates on
  outcome forests, not causal forests, and is intentionally simpler.
- **Guarded optional dependencies.** `grf` and `rlang` are the only hard
  imports; every other package (`ggplot2`, `ranger`, `glmnet`,
  `xgboost`, `dbarts`, the `mlr3` stack, `TabPFN`, `MLbalance`,
  `survival`, etc.) is `Suggests` and guarded with
  [`requireNamespace()`](https://rdrr.io/r/base/ns-load.html), so
  features degrade gracefully when a suggested package is absent. As of
  this run, `conformalInference` (a non-CRAN, GitHub-only package) has
  been fully removed from this guarded-dependency set — it is no longer
  imported, required, or referenced anywhere in `R/`, `DESCRIPTION`, or
  generated docs.
- **RNG lockstep between alternative implementations of the same
  statistical procedure.** `loco_custom_split()`’s sample-split
  (`set.seed(seed); sample(seq_len(n), floor(n/2))`) and Z-test formula
  are algebraically identical to what the removed
  [`conformalInference::loco()`](https://rdrr.io/pkg/conformalInference/man/loco.html)
  branch used to compute, because
  [`ranger::ranger()`](http://imbs-hl.github.io/ranger/reference/ranger.md)
  (when not given an explicit `seed=`) draws exactly one RNG value per
  call from R’s global stream — so two code paths performing the same
  sequence of [`sample()`](https://rdrr.io/r/base/sample.html) +
  `ranger()` calls from the same seed produce bit-identical fitted
  forests and downstream statistics. This is why the migration in this
  run produced numerically identical `method = "z"` output (see
  `log-entry.md` for the full verification record).
- **Dependency-free-by-construction internal helpers.**
  `loco_custom_split()` is a single, self-contained internal function
  reused across every ranger/grf x per-variable/group x
  regression/probability/classification combination in split mode — new
  backends or loss functions extend it rather than adding parallel
  branches.

------------------------------------------------------------------------

## Notes

- This run (`2026-07-06-loco-drop-conformal`) removed the sole
  `conformalInference`-calling branch in
  [`loco()`](https://cetialphafive.github.io/UtopiaPlanitia/reference/loco.md);
  the package now has zero code paths that reference
  `conformalInference` anywhere, and it has been dropped from
  `DESCRIPTION` `Suggests`. This is documented as an accepted,
  narrowly-scoped breaking change (numeric output on the migrated path
  is exact for `method = "z"`, within an empirically-calibrated
  tolerance for `method = "wilcox"`) — see `NEWS.md` under “Breaking
  changes” and `log-entry.md` for the full verification evidence.
- `ARCHITECTURE.md` did not exist in this repo prior to this run; this
  is the first version. Future runs should treat this file as living
  documentation and update the relevant diagram/table rather than
  regenerating it wholesale, unless a change is broad enough to warrant
  a full rebuild.
- `R/loco.R`’s `verbose` argument is retained (never removed — see the
  package’s “add, don’t change; deprecate, don’t delete” API contract in
  `CLAUDE.md`) but is now a documented inert no-op on every code path;
  it previously only ever controlled progress printing inside the
  now-removed `conformalInference` branch.
