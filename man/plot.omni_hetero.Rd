% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.omni_hetero.R
\name{plot.omni_hetero}
\alias{plot.omni_hetero}
\title{Plot an Omnibus Heterogeneity Test Object}
\usage{
\method{plot}{omni_hetero}(
  x,
  which = c("both", "blp", "rate", "toc", "folds"),
  bins = 5,
  point_alpha = 0.25,
  ...
)
}
\arguments{
\item{x}{An \code{"omni_hetero"} object (from \code{\link[=omni_hetero]{omni_hetero()}}).}

\item{which}{Character. One of \code{"both"} (default), \code{"blp"}, \code{"rate"},
\code{"toc"}, or \code{"folds"}. Selects which panel(s) to render.
\code{"rate"} returns the TOC curve stacked above the per-fold forest
plot as a \code{gtable}, with a shared note below.}

\item{bins}{Integer. Number of quantile bins of the predicted CATE used
for the BLP binned-mean overlay. Default \code{5}. Ignored for RATE panels.}

\item{point_alpha}{Alpha for the BLP per-unit scatter. Default \code{0.25}.}

\item{...}{Unused (for S3 consistency).}
}
\value{
When \code{which = "both"} or \code{"rate"}, a \code{gtable} object from
\code{\link[gridExtra:arrangeGrob]{gridExtra::arrangeGrob()}}; the grid is drawn as a side-effect when a
graphics device is open. When \code{which = "blp"}, \code{"toc"}, or \code{"folds"},
a single \code{ggplot} object with its own caption.
}
\description{
Visualizes the two preferred tests from \code{\link[=omni_hetero]{omni_hetero()}}: the
Chernozhukov et al. (2018) calibration (Best Linear Predictor) test and
the sequential RATE test of Wager (2024), plus the OOB TOC curve from
Yadlowsky et al. (2024). The default layout targets a 7.5 x 5 inch
half-page figure: BLP scatter on the left, stacked TOC / per-fold
forest plot on the right, and a shared explanatory note below.
}
\details{
The BLP panel renders the calibration regression
\deqn{\psi_i = \beta_1 \bar\tau + \beta_2 (\hat\tau_i - \bar\tau) + \epsilon_i}
(\eqn{\psi_i} = AIPW score; \eqn{\hat\tau_i} = OOB CATE) as a
partial-residual scatter for \eqn{\beta_2}.

The Sequential RATE panel renders the per-fold t-statistics computed
inside \code{\link[=omni_hetero]{omni_hetero()}}'s k-fold routine (Wager 2024) as a forest plot,
with the aggregate statistic \eqn{\sum_k t_k / \sqrt{K-1}} shown as a
diamond row. Each per-fold \eqn{t_k} is asymptotically \eqn{N(0,1)}
under H0; the aggregate is also \eqn{N(0,1)} under H0 by the
sequential validation construction of Wager (2024) (folds are \emph{not}
independent -- training sets overlap -- but the martingale argument
yields the same limiting distribution).

The OOB TOC panel draws TOC(q) = ATE among the top-q units ranked by
predicted CATE, minus the overall ATE, with a 95\% pointwise band and
reports the AUTOC (or QINI) summary from
\code{\link[grf:rank_average_treatment_effect]{grf::rank_average_treatment_effect()}}.
}
\examples{
\dontshow{if (rlang::is_installed(c("ggplot2", "gridExtra"))) withAutoprint(\{ # examplesIf}
library(grf)
set.seed(1995)
n <- 300; p <- 5
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 0.5)
Y <- X[, 1] * W + rnorm(n)
cf <- causal_forest(X, Y, W, num.trees = 200)
oh <- omni_hetero(cf)
plot(oh)                  # BLP left, TOC + fold forest stacked right
plot(oh, which = "blp")
plot(oh, which = "rate")
plot(oh, which = "toc")
plot(oh, which = "folds")
\dontshow{\}) # examplesIf}
}
\references{
Chernozhukov, V., Demirer, M., Duflo, E., and Fernandez-Val, I. (2018).
Generic Machine Learning Inference on Heterogeneous Treatment Effects in
Randomized Experiments. NBER Working Paper 24678.

Yadlowsky, S., Fleming, S., Shah, N., Brunskill, E., and Wager, S.
(2024). Evaluating Treatment Prioritization Rules via Rank-Weighted
Average Treatment Effects. JASA, forthcoming.

Wager, S. (2024). Sequential Validation of Treatment Heterogeneity.
\doi{10.48550/arXiv.2405.05534}
}
\seealso{
\code{\link[=omni_hetero]{omni_hetero()}}, \code{\link[grf:test_calibration]{grf::test_calibration()}},
\code{\link[grf:rank_average_treatment_effect]{grf::rank_average_treatment_effect()}}.
}
