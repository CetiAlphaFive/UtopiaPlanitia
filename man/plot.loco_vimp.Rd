% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loco_methods.R
\name{plot.loco_vimp}
\alias{plot.loco_vimp}
\title{Plot LOCO Variable Importance}
\usage{
\method{plot}{loco_vimp}(x, fill.sig = "darkorange", ...)
}
\arguments{
\item{x}{An object of class \code{"loco_vimp"} returned by \code{\link[=loco]{loco()}}.}

\item{fill.sig}{Bar fill for significant covariates (default
\code{"darkorange"}). Non-significant covariates use \code{"gray70"}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a horizontal variable-importance bar chart of \code{loco()} scores,
sorted from most to least important (largest at the top).
}
\details{
Shares the package house styling with \code{\link[=plot_pdp]{plot_pdp()}} (serif type, gray
panel, centered title, legend at the bottom). Colored horizontal bars
(\code{alpha = 0.75}) end in a black diamond tip point over a gray zero
reference line, with the x-axis hugged to the data. Bars are filled by
significance: a covariate is significant when its (one-sided) \code{p.value}
is below \code{alpha} (the value supplied to \code{\link[=loco]{loco()}}); significant bars use
\code{fill.sig}, others \code{"gray70"}. In split-sample mode a two-sided
confidence-interval whisker is drawn from \code{CI.lower} to \code{CI.upper}
(the diamond tip sits at \code{Importance}, which is the interval midpoint by
construction). In OOB mode (\code{split = FALSE} in the original \code{\link[=loco]{loco()}}
call) there is no confidence interval or p-value, so no whisker is drawn,
every bar takes the non-significant fill, and the title gains "(OOB)".
}
\examples{
\dontshow{if (rlang::is_installed(c("ggplot2", "ranger"))) withAutoprint(\{ # examplesIf}
set.seed(1995)
dat <- data.frame(y = rnorm(150), x1 = rnorm(150), x2 = rnorm(150),
                  x3 = rnorm(150))
dat$y <- dat$x1 + 0.5 * dat$x2 + rnorm(150, sd = 0.5)
mod <- ranger::ranger(y ~ ., data = dat, num.trees = 100)
vi <- loco(mod, split = FALSE)
plot(vi)
summary(vi)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=loco]{loco()}} to compute the scores, \code{\link[=summary.loco_vimp]{summary.loco_vimp()}} for
tabular output.
}
