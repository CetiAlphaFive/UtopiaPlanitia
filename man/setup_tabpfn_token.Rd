% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_tabpfn_token.R
\name{setup_tabpfn_token}
\alias{setup_tabpfn_token}
\title{Set up the TABPFN_TOKEN environment variable}
\usage{
setup_tabpfn_token(
  token = NULL,
  persist = NULL,
  overwrite = FALSE,
  renviron_path = path.expand("~/.Renviron")
)
}
\arguments{
\item{token}{Optional character scalar. If supplied, skips the
prompt and uses this value directly (still validated and still
subject to \code{persist}).}

\item{persist}{Optional logical. If \code{TRUE}, append the token to
\code{renviron_path} (creating the file if absent). If \code{FALSE}, set
only for the current session. If \code{NULL} (default), the user is
asked interactively.}

\item{overwrite}{Logical. If \code{FALSE} (default), an existing
\code{TABPFN_TOKEN} short-circuits the function. If \code{TRUE}, the
function proceeds even when a token is already set.}

\item{renviron_path}{Path to the \code{.Renviron} file used when
\code{persist = TRUE}. Defaults to \code{path.expand("~/.Renviron")}. Exposed
primarily for testing.}
}
\value{
Invisibly, \code{TRUE} on success and \code{FALSE} if the user
provided an invalid token. May also \code{stop()} in non-interactive
sessions when the token is missing.
}
\description{
Interactive helper that checks for \code{TABPFN_TOKEN}, walks the user
through obtaining one from \url{https://ux.priorlabs.ai/account}, and
optionally persists it to \verb{~/.Renviron} for future R sessions.
}
\details{
Behaviour:
\itemize{
\item If \code{TABPFN_TOKEN} is already set and \code{overwrite = FALSE},
the function reports the length of the existing token and
returns \code{invisible(TRUE)} without prompting.
\item If the session is non-interactive and the token is missing,
the function \code{stop()}s with a pointer to
\url{https://ux.priorlabs.ai/account}.
\item Otherwise the function prints short instructions, optionally
opens a browser, prompts for the token (using
\code{askpass::askpass()} if available), validates it, sets the
environment variable for the current session, and optionally
appends it to \verb{~/.Renviron}.
}

Token validation rejects empty tokens, tokens containing whitespace,
and tokens shorter than 11 characters. A rejected token returns
\code{invisible(FALSE)} and does not change \code{Sys.getenv("TABPFN_TOKEN")}.
}
\examples{
\dontrun{
# Check first, prompt only if missing:
setup_tabpfn_token()

# Force re-entry even if a token is already set:
setup_tabpfn_token(overwrite = TRUE)

# Non-interactive use (e.g. in a script with a token from a vault):
setup_tabpfn_token(token = Sys.getenv("MY_VAULT_TOKEN"),
                   persist = FALSE)
}
}
