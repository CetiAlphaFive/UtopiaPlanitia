% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gates.R
\name{plot_gates}
\alias{plot_gates}
\title{Plot GATES Estimates}
\usage{
plot_gates(
  x,
  groups = "all",
  show.ate = TRUE,
  y.limits = NULL,
  title = "Sorted Group Average Treatment Effects",
  ...
)
}
\arguments{
\item{x}{An object of class \code{"cf_gates"} returned by \code{\link[=gates]{gates()}}.}

\item{groups}{Character vector of group labels to plot. Default \code{"all"} plots
every row in \code{x$groups} and \code{x$diff}. Otherwise a subset of names like
\code{c("G1", "G4", "G4-G1")}.}

\item{show.ate}{Logical. If \code{TRUE} (default), draw horizontal reference lines
for the overall ATE and its confidence interval from \code{x$ate}.}

\item{y.limits}{Optional numeric vector of length two for the y-axis limits.
Default spans the plotted intervals (and ATE band when \code{show.ate = TRUE}),
always including zero.}

\item{title}{Optional plot title. Default \code{"Sorted Group Average Treatment Effects"}.}

\item{...}{Unused (for consistency with other plotters).}
}
\value{
A \code{utopia_plot} object (ggplot grob tagged for \code{\link[=print.utopia_plot]{print.utopia_plot()}}).
}
\description{
Visualizes sorted group average treatment effect estimates from \code{\link[=gates]{gates()}}
as a point-and-interval plot by heterogeneity group, with optional reference
lines for the overall average treatment effect and zero.
}
\details{
Shares the package house styling with \code{\link[=plot_pdp]{plot_pdp()}} (serif type, gray panel,
centered title). Groups are ordered on the x-axis from lowest to highest
predicted CATE (\code{G1} .. \code{GK}).
}
\examples{
\dontshow{if (rlang::is_installed("ggplot2")) withAutoprint(\{ # examplesIf}
library(grf)
set.seed(1995)
n <- 400; p <- 5
X <- matrix(rnorm(n * p), n, p)
colnames(X) <- paste0("X", seq_len(p))
W <- rbinom(n, 1, 0.5)
Y <- X[, 1] * W + rnorm(n)
cf <- causal_forest(X, Y, W, num.trees = 200)
g <- gates(cf)
plot_gates(g)
\dontshow{\}) # examplesIf}
}
\references{
Chernozhukov, V., Demirer, M., Duflo, E., and Fernandez-Val, I. (2020).
Generic Machine Learning Inference on Heterogeneous Treatment Effects in
Randomized Experiments. \emph{Econometrica}, forthcoming.
}
\seealso{
\code{\link[=gates]{gates()}}, \code{\link[=print.cf_gates]{print.cf_gates()}}
}
