% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.causal_forest.R
\name{plot.causal_forest}
\alias{plot.causal_forest}
\title{Plot a Causal Forest}
\usage{
\method{plot}{causal_forest}(x, type = c("diag", "pdp", "scatter", "rank"), ...)
}
\arguments{
\item{x}{A fitted causal forest object from the \code{grf} package.}

\item{type}{Character string specifying the plot type. One of:
\itemize{
\item \code{"diag"}: multi-panel diagnostics (\code{\link[=plot_diag]{plot_diag()}})
\item \code{"pdp"}: partial dependence plot (\code{\link[=plot_pdp]{plot_pdp()}})
\item \code{"scatter"}: individual CATE scatter (\code{\link[=plot_scatter]{plot_scatter()}})
\item \code{"rank"}: ranked CATEs with CIs (\code{\link[=plot_rank]{plot_rank()}})
}}

\item{...}{Additional arguments passed to the underlying plot function
(e.g., \code{x_var} for \code{"pdp"} and \code{"scatter"}).}
}
\value{
A plot object (the return value of the dispatched function).
}
\description{
Dispatches to one of the package's plot functions based on \code{type}.
}
\details{
This is a convenience dispatcher. Each type targets a different diagnostic
question:
\describe{
\item{\code{"diag"}}{Are the nuisance models (outcome, propensity) well
calibrated? Is there treatment effect heterogeneity?}
\item{\code{"pdp"}}{How does the CATE change as a function of one or two
covariates, marginalizing over the rest?}
\item{\code{"scatter"}}{What do the individual OOB CATE predictions look
like against a single covariate?}
\item{\code{"rank"}}{What is the distribution of individual CATEs, sorted
by magnitude, with confidence intervals?}
}
}
\examples{
\dontshow{if (rlang::is_installed(c("ggplot2", "ggdist"))) withAutoprint(\{ # examplesIf}
library(grf)
set.seed(1995)
n <- 200; p <- 5
X <- matrix(rnorm(n * p), n, p)
colnames(X) <- paste0("X", seq_len(p))
W <- rbinom(n, 1, 0.5)
Y <- X[, 1] * W + rnorm(n)
cf <- causal_forest(X, Y, W, num.trees = 100)
plot(cf, type = "rank")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_diag]{plot_diag()}}, \code{\link[=plot_pdp]{plot_pdp()}}, \code{\link[=plot_scatter]{plot_scatter()}}, \code{\link[=plot_rank]{plot_rank()}}
}
